---
// PLANTILLA PARA CREAR PÁGINAS DE SEO LOCAL POR CIUDAD
// Copia este archivo y renómbralo según el patrón: menu-digital-restaurantes-[ciudad].astro
// Por ejemplo: menu-digital-restaurantes-santiago.astro

import SeoLayout from "../layouts/SeoLayout.astro";
import Hero from "../components/sections/Hero.astro";
import Problems from "../components/sections/Problems.astro";
import HowItWorks from "../components/sections/HowItWorks.astro";
import Benefits from "../components/sections/Benefits.astro";
import CTA from "../components/sections/CTA.astro";
import FadeIn from "../components/ui/FadeIn.astro";
import Breadcrumbs from "../components/ui/Breadcrumbs.astro";

import { 
  getCityData, 
  generateLocalBusinessSchema,
  generateBreadcrumbSchema 
} from "../data/cities";

// 👇 PASO 1: Cambia el slug de la ciudad aquí
const CITY_SLUG = "NOMBRE_CIUDAD"; // Ej: "santiago", "concepcion", "valparaiso", etc.

const cityData = getCityData(CITY_SLUG);
if (!cityData) {
  return Astro.redirect("/404");
}

const pageUrl = `https://menupro.cl/menu-digital-restaurantes-${cityData.slug}`;

// Schema.org para SEO local
const schemas = {
  "@context": "https://schema.org",
  "@graph": [
    generateLocalBusinessSchema(cityData),
    generateBreadcrumbSchema(cityData, pageUrl),
    {
      "@type": "WebPage",
      "name": `Menú Digital para Restaurantes en ${cityData.name}`,
      "description": cityData.description,
      "url": pageUrl
    }
  ]
};
---

<SeoLayout
  title={`Menú Digital para Restaurantes en ${cityData.name} | MenuPro`}
  description={cityData.description}
  canonical={pageUrl}
  keywords={cityData.keywords}
  city={cityData.name}
  schema={schemas}
  ogType="website"
  ogImage={cityData.ogImage}
>
  <Breadcrumbs items={[
    { label: "Inicio", href: "/" },
    { label: "Menú Digital", href: "/menu-digital-restaurantes" },
    { label: cityData.name }
  ]} />
  
  <Hero
    title={`Menú Digital para Restaurantes en ${cityData.name}`}
    subtitle="Crea y administra tu carta digital con QR de forma simple, sin reimpresiones ni terceros."
  />

  <!-- Sección Local Contextual -->
  <section class="py-16 px-6 max-w-5xl mx-auto">
    <FadeIn>
      <div class="bg-[var(--color-bg-soft)] rounded-3xl p-8 md:p-12 border border-default">
        <h2 class="text-2xl md:text-3xl font-semibold mb-6 text-center">
          MenuPro para restaurantes en {cityData.name}
        </h2>
        
        <div class="prose prose-lg dark:prose-invert max-w-none">
          <p class="text-muted text-center mb-8">
            {cityData.localContext}
          </p>

          <div class="grid md:grid-cols-2 gap-6 mt-8">
            <div class="bg-white dark:bg-neutral-900 rounded-2xl p-6 shadow-sm">
              <h3 class="text-lg font-semibold mb-3 flex items-center gap-2">
                <span class="text-2xl">🍽️</span>
                Ideal para tu restaurante
              </h3>
              <p class="text-sm text-muted">
                Ya sea que tengas un restaurant en el centro de {cityData.name}, 
                una picada familiar o un café con vista al río, MenuPro se adapta 
                a tu negocio. Sin complicaciones técnicas.
              </p>
            </div>

            <div class="bg-white dark:bg-neutral-900 rounded-2xl p-6 shadow-sm">
              <h3 class="text-lg font-semibold mb-3 flex items-center gap-2">
                <span class="text-2xl">📱</span>
                Actualización en tiempo real
              </h3>
              <p class="text-sm text-muted">
                Cambia precios, agrega platos del día o actualiza disponibilidad 
                desde cualquier lugar. Tus clientes siempre verán la carta más reciente.
              </p>
            </div>

            <div class="bg-white dark:bg-neutral-900 rounded-2xl p-6 shadow-sm">
              <h3 class="text-lg font-semibold mb-3 flex items-center gap-2">
                <span class="text-2xl">💰</span>
                Sin costos ocultos
              </h3>
              <p class="text-sm text-muted">
                Un solo pago mensual fijo. No hay comisiones por pedido, 
                ni sorpresas en la boleta. Transparencia total.
              </p>
            </div>

            <div class="bg-white dark:bg-neutral-900 rounded-2xl p-6 shadow-sm">
              <h3 class="text-lg font-semibold mb-3 flex items-center gap-2">
                <span class="text-2xl">🎯</span>
                Soporte local
              </h3>
              <p class="text-sm text-muted">
                Atención en español, horarios compatibles con Chile, 
                y entendimiento real de cómo funciona la gastronomía local.
              </p>
            </div>
          </div>

          {cityData.neighborhoods && cityData.neighborhoods.length > 0 && (
            <div class="mt-8 text-center">
              <p class="text-sm text-muted">
                <strong>Atendemos todo {cityData.name}:</strong> {cityData.neighborhoods.join(", ")}
              </p>
            </div>
          )}
        </div>
      </div>
    </FadeIn>
  </section>

  <Problems />
  <HowItWorks />
  <Benefits city={cityData.name} />
  <CTA city={cityData.name} />
</SeoLayout>
